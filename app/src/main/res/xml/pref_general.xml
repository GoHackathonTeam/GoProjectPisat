<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="2"
        android:entries="@array/pref_start_activity_list_titles"
        android:entryValues="@array/pref_start_activity_list_values"
        android:key="start_activity"
        android:title="@string/pref_start_activity" />

    <ListPreference
        android:defaultValue="2"
        android:entries="@array/pref_schedule_types_titles"
        android:entryValues="@array/pref_schedule_types_values"
        android:key="schedule_type"
        android:title="@string/pref_schedule_type" />

</PreferenceScreen>
